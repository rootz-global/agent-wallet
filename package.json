{
  "name": "@rootz/agent-wallet",
  "version": "0.1.0",
  "description": "SDK for AI agents with provable origin — birth certificates, signed chains, session archives. npm install and go.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@rootz/agent-runtime": "*",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
